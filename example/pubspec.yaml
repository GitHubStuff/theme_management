name: xample
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.1+4

environment:
  sdk: ">=2.16.0 <3.0.0" #Dart SDK 2022-02-04
  flutter: ">=2.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  extensions_package:
    git:
      url: https://github.com/RAE-Health/extensions_package.git
  flutter_bloc: ^8.0.1
  flutter_localizations:
    sdk: flutter
  widget_tree_package:
    git:
      url: https://github.com/RAE-Health/widget_tree_package.git
  flutter_modular: ^4.4.0
  language_package:
    git:
      url: https://github.com/RAE-Health/language_package.git
  rae_localization_package:
    git:
      url: https://github.com/RAE-Health/rae_localization_package.git


dependency_overrides:
  theme_management:
    path: ../

dev_dependencies:
  test:
  build_runner:

flutter:
  uses-material-design: true
  generate: true
